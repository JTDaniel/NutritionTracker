# API proxy is handled by functions/api/[[path]].js (Pages Function)
# Set TUNNEL_URL environment variable in Cloudflare Pages dashboard

# SPA fallback — all routes serve index.html
/*    /index.html    200
