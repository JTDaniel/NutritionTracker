# Proxy all API requests to the local backend via Cloudflare Tunnel
# Update this URL each time the tunnel restarts (or use a named tunnel with a domain for a stable URL)
# Then commit and push — Cloudflare Pages auto-redeploys in ~1 minute
/api/* https://YOUR-TUNNEL-URL.trycloudflare.com/api/:splat 200
